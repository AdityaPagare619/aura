# ============================================================
# AURA v4 — CI Pipeline v2
# 6-Stage Pipeline for Cross-Compilation and Device Testing
#
# STAGE 1: SOURCE — Checkout, cache, env detection
# STAGE 2: BUILD  — Cross-compile for ARM64 (Android)
# STAGE 3: INSPECT — Verify artifact architecture
# STAGE 4: TEST   — Unit tests + lint (Linux)
# STAGE 5: DEPLOY — Upload artifact
# STAGE 6: DEVICE — Execute on device (device-validate.yml)
#
# TOOLCHAIN: stable (was nightly-2026-03-01 — F001 fix: NDK #2073)
# ============================================================

name: CI Pipeline v2

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 3 * * *'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  RUST_BACKTRACE: full

jobs:
  # ============================================================
  # STAGE 1: SOURCE — Checkout, cache, env detection
  # ============================================================
  source:
    name: Source
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable
          targets: aarch64-linux-android

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Detect environment
        run: |
          echo "HOST=$(uname -s)"
          echo "ARCH=$(uname -m)"
          echo "TARGET=aarch64-linux-android"
          echo "RUSTC=$(rustc --version)"
          echo "CARGO=$(cargo --version)"

  # ============================================================
  # STAGE 2: BUILD — Cross-compile for ARM64 (Android)
  # ============================================================
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable
          targets: aarch64-linux-android

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential pkg-config libopus-dev

      # STAGE 2: BUILD — Cross-compile aura-daemon for Android ARM64
      # reqwest feature: use reqwest HTTP backend (CI/Linux compatible)
      # stub feature: skip llama.cpp FFI (no NDK for llama.cpp)
      - name: Build aura-daemon (Android ARM64)
        run: |
          cargo build \
            --release \
            --features "aura-llama-sys/stub,aura-daemon/reqwest" \
            --target aarch64-linux-android \
            --manifest-path crates/aura-daemon/Cargo.toml

      - name: Build aura-cli (Android ARM64)
        run: |
          cargo build \
            --release \
            --target aarch64-linux-android \
            --manifest-path crates/aura-cli/Cargo.toml

      # ============================================================
      # STAGE 3: INSPECT — Verify artifact architecture
      # ============================================================
      - name: Verify architecture
        run: |
          echo "=== Verifying aura-daemon artifact ==="
          file targets/aarch64-linux-android/release/aura-daemon
          # Expected: ELF 64-bit LSB executable, ARM aarch64

      - name: Verify ELF header
        run: |
          echo "=== ELF Header ==="
          readelf -h targets/aarch64-linux-android/release/aura-daemon | grep -E "(Machine|Class)"
          # Expected: Machine: AArch64

      - name: Verify dynamic linker (bionic, NOT glibc)
        run: |
          echo "=== Dynamic Linker ==="
          readelf -d targets/aarch64-linux-android/release/aura-daemon | grep NEEDED
          # Expected: libc.so, libm.so, libdl.so (bionic)

      - name: Check for x86_64 (should NOT exist)
        run: |
          ARCH=$(file targets/aarch64-linux-android/release/aura-daemon | grep -o "x86-64")
          if [ -n "$ARCH" ]; then
            echo "::error::WRONG ARCHITECTURE: Found x86-64 in ARM64 build!"
            exit 1
          fi
          echo "Architecture check passed"

      # ============================================================
      # STAGE 5: DEPLOY — Upload artifact
      # ============================================================
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aura-daemon-android-v2
          path: |
            targets/aarch64-linux-android/release/aura-daemon
            targets/aarch64-linux-android/release/aura-cli
          retention-days: 30

  # ============================================================
  # STAGE 4: TEST — Unit tests + lint (Linux)
  # ============================================================
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable
          components: clippy, rustfmt

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential pkg-config libopus-dev

      - run: cargo check --workspace --features "aura-llama-sys/stub,aura-daemon/voice,aura-daemon/reqwest"

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential pkg-config libopus-dev

      - run: cargo test --workspace --features "aura-llama-sys/stub,aura-daemon/voice,aura-daemon/reqwest"
        env:
          RUST_LOG: debug

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Install Rust (stable) + clippy
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential pkg-config libopus-dev

      - run: cargo clippy --workspace --features "aura-llama-sys/stub,aura-daemon/voice,aura-daemon/reqwest" -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive

      - name: Install Rust (stable) + rustfmt
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable
          components: rustfmt

      - run: cargo fmt --check

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master
        with:
          toolchain: stable

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Run security audit
        run: |
          cargo audit --ignore RUSTSEC-2025-0141

  version-check:
    name: Version Tag Check
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Validate version tag matches Cargo.toml
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"
          CARGO_VERSION=$(grep -m1 '^version' Cargo.toml | sed 's/.*"\(.*\)"/\1/')
          echo "Git tag version:   $TAG_VERSION"
          echo "Cargo.toml version: $CARGO_VERSION"
          if [ "$TAG_VERSION" != "$CARGO_VERSION" ]; then
            echo "::error::Version mismatch! Tag v${TAG_VERSION} does not match Cargo.toml version ${CARGO_VERSION}"
            exit 1
          fi
          echo "Versions match."
